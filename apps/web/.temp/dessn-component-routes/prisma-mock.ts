export enum SchedulingType {
  ROUND_ROBIN = 'ROUND_ROBIN',
  COLLECTIVE = 'COLLECTIVE',
  MANAGED = 'MANAGED'
}

export const PrismaClient = {
  eventType: {
    findMany: async () => [],
    update: async () => ({})
  }
};